import React from 'react';
//import AppTopbar from 'components/AppTopbar';





export default function Dashboard() {
  return (
    <>
      
      <div style={{ padding: 24 }}>
        <h1>Dashboard</h1>
        <p>Coming soon…</p>
      </div>
    </>
  );
}

//make a unified dashboard for all sisters w events and point values (candidates have diff events, so they need their own diff dahsboard,
//alumni also have a comletely diff set of events so they get their own dashbaord??)
//officers will have buttons built on top of dashboard for event creation and management--> new pages?
// candidates have their own different events and their own class officers  for their dashboards
  //dont need to worry about candidate accounts until spring sem when rush occurs
      //unless we want those interested in recruiting in fall to have accounts (pnm-- potential new member) and be able to sign up for text reminders
      //should be able to convert pnm account into a candidate account if we select them as candidates
